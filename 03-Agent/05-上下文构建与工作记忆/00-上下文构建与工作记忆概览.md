---
type: topic-overview
module: 3
status: planned
audience: non-specialist
parent: "[[00-Agent模块大纲|Agent 模块大纲]]"
tags: [agent, context, memory, retrieval, compaction]
---

# 上下文构建与工作记忆概览

> [!summary]
> Agent 的外部状态并不会自动进入 LLM；Context Builder 必须在每次调用前选择当前阶段真正需要的信息，并把它组织成模型能够读取的上下文。

## 主线位置

```text
持久状态 + 当前 Observation + 项目资料 + 策略
→ 选择、验证、排序和裁剪
→ Context Package
→ 模型服务的 Context Builder
→ Tokenizer
→ LLM
```

## 本专题要解决的问题

- State、Context、Session、Thread、Memory 有什么区别；
- 为什么不能默认重放全部历史；
- Context Builder 与 Context Compiler 分别强调什么；
- Summary、Retrieval 和 Compaction 各解决什么问题；
- 怎样标注事实来源、版本和可信级别；
- 新线程、续接线程和隔离上下文怎样选择；
- 上下文窗口未满为什么仍可能出现注意力干扰。

## 工作记忆的准确边界

本专题中的工作记忆主要指“当前任务可再次取用的信息”。它不等于模型 Weight，也不自动构成跨任务持续学习。

## 后续展开

1. 状态怎样变成本轮 Context；
2. 系统指令、Goal、历史、工具结果和资料的排序；
3. Context Package 与 Manifest；
4. Retrieval、摘要与压缩；
5. 新线程、上下文隔离与共享；
6. 上下文污染、过期和冲突处理。

## 阅读路线

- 只看框架：本页；
- 理解机制：能够从一个项目状态挑出某一阶段所需信息；
- 为搭建做准备：让关键状态可重新编译，而不是依赖模型记住整段对话。
