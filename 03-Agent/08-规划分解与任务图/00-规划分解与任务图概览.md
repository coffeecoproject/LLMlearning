---
type: topic-overview
module: 3
status: planned
audience: non-specialist
parent: "[[00-Agent模块大纲|Agent 模块大纲]]"
tags: [agent, planning, decomposition, task-graph]
---

# 规划、分解与任务图概览

> [!summary]
> 规划把 Goal 转成可执行、可验证且具有依赖关系的任务；它可以由模型提议，但任务图、约束和完成状态需要由外部系统保存和校验。

## 从目标到执行

```text
Goal
→ 识别约束和未知项
→ 分解 Tasks
→ 建立依赖关系
→ 选择可执行任务
→ 执行并读取反馈
→ 必要时重新规划
```

## 本专题要解决的问题

- Goal、Plan、Task、Step 和 Workflow 有什么区别；
- 计划应该一次生成完，还是随着观察逐步形成；
- 顺序列表、状态机、有向无环图（Directed Acyclic Graph，DAG）和一般任务图分别适合什么任务；
- 如何表示依赖、阻塞、并行和共享资源；
- 计划变化怎样避免越出 Goal Scope；
- 为什么“模型写了一份计划”不等于系统已经拥有可执行计划；
- Graph Engineering 与普通 Prompt 编排的边界。

## 关键边界

计划是执行假设，不是事实。新 Observation 可以使计划失效，但重新规划不能绕过用户约束、权限和成功条件。

## 后续展开

1. 任务分解的粒度；
2. 静态计划与动态规划；
3. 依赖图、并行与阻塞；
4. 计划版本和重新规划；
5. 计划与上下文选择；
6. Graph、Workflow 和 Agent 的组合。

## 阅读路线

- 只看框架：本页可选；
- 理解机制：能够把一个 Goal 分成带依赖和验收条件的 Tasks；
- 为搭建做准备：先使用简单状态机或任务列表，只有依赖复杂时再引入图。
