---
type: topic-overview
module: 3
status: complete
audience: non-specialist
parent: "[[00-Agent模块大纲|Agent 模块大纲]]"
tags: [agent, goal, intake, success-criteria, scope]
---

# 目标与任务契约概览

> [!summary]
> Agent 至少需要一个可持续追踪的任务目标；对于模糊、长期或高风险任务，还应把自然语言需求转成可确认、可修订和可验收的任务契约，否则系统可能非常认真地完成错误任务。

## Goal 有不同控制深度

下面是本教程为了比较控制深度使用的学习分类，不是行业统一命名。

```text
Prompt-bound Goal
→ 直接把用户请求作为当前任务目标

Thread Goal
→ 把 Objective 和运行状态持久化到某个线程

Goal Contract
→ 把 Objective、Success Criteria、Scope、Non-goals、revision 和验收关系结构化
```

三者都可以出现在 Agent 中。Goal Intake 不是“Agent”成立的必要条件，而是从模糊需求形成高可靠 Goal Contract 的增强模块。

## 在高可靠 Agent 主线中的位置

```text
用户原始需求
→ 形成 Goal Draft
→ 识别歧义与缺失条件
→ 必要时进行有限只读探索
→ 用户确认关键业务含义
→ 创建正式 Goal
→ 受控 Agent Run 获得正式执行依据
```

这是高可靠控制型 Agent 的推荐顺序。简单任务可以直接把用户请求作为目标；持续 Agent 可以先保存一个 Thread Goal；当需求模糊、影响范围广或验收风险较高时，再使用这里的完整 Goal Intake。它回答的是“究竟要完成什么”，而正式 Discovery、Planning 和 Tool Use 回答的是“为了完成它，需要了解什么以及怎样行动”。

## 为什么称为任务契约

正式 Goal 不只是把用户原话保存下来，还需要至少区分：

```text
Objective
→ 要改变或实现什么

Success Criteria
→ 出现哪些可观察结果才算成功

Scope
→ 本次任务必须覆盖或允许影响什么

Non-goals
→ 哪些事情明确不属于本次任务
```

它们共同约束后续的搜索范围、计划、工具权限、实际变更和最终验收。

## 高可靠任务契约的推荐结构

```text
Raw Request
    ↓
Goal Intake
    ├── 生成 Goal Draft
    ├── 识别歧义
    ├── 提出澄清问题
    └── 展示待确认内容
            ↓
Human Confirmation
            ↓
Goal Manager
    └── 创建或修订正式 Goal
            ↓
Workflow / Agent Runtime
```

Goal Intake 可以使用 LLM 帮助理解需求；Goal Manager 才拥有正式 Goal 的创建和修订权威。

## 真实实现观察：Codex CLI

Codex CLI 当前已经提供持久化的 Thread Goal：它保存 Objective、状态、可选 Token 预算以及 Token/时间使用量，并可以跨 Turn 继续工作。但它没有把 Success Criteria、Scope、Non-goals、Goal revision 和 Evidence 关系拆成同等级的正式字段，也没有强制执行独立 Goal Intake。

因此 Codex 属于“持续 Agent”实现，而本专题后半部分主要研究怎样在它之上构建更严格的 Goal Contract。详细对照见 [[01-Codex-Thread-Goal与高可靠Goal契约|Codex Thread Goal 与高可靠 Goal 契约]]。

## 四层阅读结构

### 只看框架

1. [[00-Goal为什么是Agent入口|Goal 为什么是 Agent 的入口]]；
2. [[01-Goal的四个核心部分|Goal 的四个核心部分]]。

读完后应能解释 Goal 为什么不是普通 Prompt。

### 理解机制

1. [[00-从模糊需求到正式Goal概览|从模糊需求到正式 Goal 概览]]；
2. [[01-原始需求Goal-Draft与正式Goal|原始需求、Goal Draft 与正式 Goal]]；
3. [[02-成功条件怎样变得可验证|成功条件怎样变得可验证]]；
4. [[03-Goal-Task-Plan与Workflow的关系|Goal、Task、Plan 与 Workflow 的关系]]；
5. [[04-Goal如何约束后续执行|Goal 如何约束后续执行]]。

### 为搭建做准备

1. [[00-Goal-Intake搭建概览|Goal Intake 搭建概览]]；
2. [[01-歧义识别澄清与用户确认|歧义识别、澄清与用户确认]]；
3. [[02-Goal-Revision与失效传播|Goal Revision 与失效传播]]；
4. [[03-Goal-Schema与最小状态流|Goal Schema 与最小状态流]]。

### 案例与复习

- [[00-目标与任务契约案例复习|目标与任务契约案例复习]]。
- [[01-Codex-Thread-Goal与高可靠Goal契约|Codex Thread Goal 与高可靠 Goal 契约]]。

## 关键边界

- LLM 可以提出 Goal Draft，但不能自行确认用户真实意图；
- 简单 Agent 可以直接使用用户请求，Goal Intake 不应被描述为所有 Agent 的必经步骤；
- Goal 负责定义目标边界，不负责提前写死全部执行步骤；
- Success Criteria 是验收输入，不是模型生成的一句漂亮结果描述；
- Discovery 可以发现新事实，但不能偷偷改变 Goal；
- Goal 改变需要新 revision，并检查计划、上下文、产物和证据是否已经过期；
- 形式合法不等于语义充分，非空字段仍可能表达一个很差的 Goal。

## 框架停止点

如果你能根据一句模糊需求写出 Objective、Success Criteria、Scope 和 Non-goals，并指出其中哪些内容必须由用户确认，本专题在框架层已经完成，可以进入 [[00-Agent循环与生命周期概览|Agent 循环与生命周期概览]]。
