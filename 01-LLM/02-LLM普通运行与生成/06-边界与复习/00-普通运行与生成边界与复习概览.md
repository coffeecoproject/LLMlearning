---
type: topic-index
module: 1
status: active
audience: non-specialist
parent: "[[00-LLM普通运行与生成大纲|LLM普通运行与生成大纲]]"
previous: "[[00-Runtime服务系统概览|Runtime服务系统概览]]"
tags: [llm, inference, boundaries, review]
---

# 普通运行与生成边界与复习概览

> [!summary]
> 本专题把普通运行重新放回完整系统：参数固定不等于没有临时状态；模型生成不等于 Runtime 服务；一次 API 调用也不等于完整 Agent。

## 已有边界笔记

1. [[01-训练完成后怎样使用Embedding|训练完成后怎样使用 Embedding]] ✓

## 后续复习结构

1. 训练、普通运行和持续学习的区别；
2. 模型、生成控制器、Runtime、API 与客户端的职责；
3. 单次 LLM 请求与 Agent 执行循环的区别；
4. 从 Messages 到 Streaming 的完整复盘；
5. 常见误解与理解检查。

## 当前最重要的四条边界

```text
上下文适应 ≠ 参数更新
KV Cache ≠ 长期记忆
逐 Token 生成 ≠ Agent 多步执行
服务端批处理 ≠ 一个回答的未来 Token 同时生成
```

> [!info] 当前进度
> 已迁入基础边界笔记；完整复习将在前三个后续专题完成后补齐。
