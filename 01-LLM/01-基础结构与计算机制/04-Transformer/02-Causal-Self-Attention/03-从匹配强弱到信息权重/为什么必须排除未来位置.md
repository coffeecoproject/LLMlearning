---
type: concept
module: 1
status: complete
audience: non-specialist
parent: "[[从匹配强弱到信息权重概览]]"
previous: "[[Score是什么]]"
next: "[[Weight是什么]]"
tags: [llm, attention, causal-mask, decoder-only]
---

# 为什么必须排除未来位置

> [!summary]
> 在 Causal Self-Attention 中，位置 `i` 的表示只允许依赖位置 `0` 到 `i`；未来位置不属于它的可见上下文，因此必须被排除。

## 先理解这是一条结构规则

`Causal` 表示信息只能沿着序列的因果方向流动：过去可以影响后面，后面的内容不能反过来进入前面位置的表示。

```text
过去位置 ──可以──→ 当前及后续位置
未来位置 ─不可以→ 更早位置
```

如果位置 1 可以读取位置 3，那么位置 1 的表示已经包含了“未来才出现的信息”，这个网络就不再满足 Causal Attention 的定义。至于这条规则在训练和逐 Token 生成中分别解决什么问题，留到对应阶段模块。

## 可见规则

长度为 4 的序列：

```text
位置 0 → 只能看 0
位置 1 → 可以看 0、1
位置 2 → 可以看 0、1、2
位置 3 → 可以看 0、1、2、3
```

这就是 Causal Mask 建立的方向：可以看自己和过去，不能看未来。

## 为什么不能只给未来位置较低分

未来信息不是“不太推荐”，而是“不允许使用”。即使某个未来 Token 与当前位置高度匹配，也必须让它最终获得 0 权重。

这与 ALiBi 不同：

```text
ALiBi
→ 对远距离位置施加软偏置，仍可能被关注

Causal Mask
→ 对未来位置施加硬约束，不能被关注
```

因此 Mask 处理的是“有没有读取权限”，Score 处理的是“在有权限的来源中匹配得多强”。这两个问题不能合并。

## 与 Padding Mask 区分

```text
Causal Mask：排除未来位置
Padding Mask：排除为了对齐形状加入的无效位置
```

实现可能合并两种 Mask，但它们解决的问题不同。

## 可选技术补充：极小值

程序通常在 Softmax 前把被禁止位置的 Score 改成负无穷或数据类型可表示的极小值：

```text
[2.0, 1.0, 3.0]
→ [2.0, 1.0, 禁止]
```

经过 Softmax 后，禁止位置的权重为 0。记住“禁止 → 权重为 0”即可，不要求计算指数函数。

## 常见误解

- **“Mask 会从输入中删除未来 Token。”** 它限制 Attention 关系，不必删除张量中的 Token。
- **“未来位置只是受到普通扣分。”** Causal Mask 的目标是让其权重为 0。
- **“ALiBi 可以代替 Causal Mask。”** 软距离偏置不能替代未来硬屏蔽。
- **“Padding 和未来位置都是一回事。”** 一个是无效填充，一个是因果限制。

## 理解检查

1. 位置 2 为什么不能读取位置 3？
2. Causal Mask 与 ALiBi 的约束强度有什么区别？
3. Padding Mask 与 Causal Mask 分别排除什么？

下一篇：[[Weight是什么|Weight 是什么]]。
