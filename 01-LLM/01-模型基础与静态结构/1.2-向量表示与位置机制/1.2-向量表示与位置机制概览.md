---
type: topic-index
module: 1
section: 1.2
status: complete
audience: non-specialist
parent: "[[01-LLM/01-模型基础与静态结构/模型基础与静态结构大纲]]"
previous: "[[1.1-Tokenizer文本离散化系统概览]]"
tags: [llm, embedding, vector, position]
---

# 1.2 向量表示与位置机制

> [!goal]
> 理解 Token ID 怎样变成 Transformer 可以计算的初始向量，以及模型为什么还需要以不同方式获得 Token 的位置信息。

## 与上一专题的连接

[[1.1-Tokenizer文本离散化系统概览|Tokenizer]] 的输出是整数编号：

```text
文本
→ Tokenizer
→ Token ID 序列
```

但 ID 只是词表中的索引。例如 ID `10001` 不表示它比 ID `10000` “语义更大”，也不能直接用编号之间的距离表达词义。因此模型还需要：

```text
Token ID
→ Embedding Lookup
→ 初始向量
→ 获得或注入位置信息
→ 进入 Transformer Block
```

> [!note]
> `10001` 只是示意 ID，不来自具体模型。

## 子结构与学习顺序

1. [[1.2.1-向量基础概览|1.2.1 向量基础]]：先理解向量、维度、距离与张量的最低必要知识。
2. [[1.2.2-Token-Embedding概览|1.2.2 Token Embedding]]：理解 Embedding Matrix 与 Lookup。
3. [[1.2.3-Embedding参数与语义边界概览|1.2.3 Embedding 参数与语义边界]]：认识 Embedding 的参数属性和语义边界。
4. [[1.2.4-初始表示与Hidden-State概览|1.2.4 初始表示与 Hidden State]]：区分上下文前后的表示。
5. [[1.2.5-位置机制概览|1.2.5 位置机制]]：理解位置为何必要，以及 Absolute Position、RoPE、ALiBi 作用在哪里。

## 完整知识结构

```text
1.2 向量表示与位置机制
│
├── 1.2.1 向量基础
│   ├── 1.2.1.1 向量是什么
│   ├── 1.2.1.2 维度与 Hidden Size
│   ├── 1.2.1.3 向量距离与相似性
│   └── 1.2.1.4 从单个向量到序列张量
│
├── 1.2.2 Token Embedding
│   ├── 1.2.2.1 Token ID 为什么不能直接计算语义
│   ├── 1.2.2.2 Embedding Matrix
│   ├── 1.2.2.3 Embedding Lookup
│   └── 1.2.2.4 形状、词表规模与参数量
│
├── 1.2.3 Embedding 参数与语义边界
│   ├── 1.2.3.1 Embedding 是可学习参数
│   └── 1.2.3.2 Embedding Space 与语义边界
│
├── 1.2.4 初始表示与 Hidden State
│   ├── 1.2.4.1 初始 Token Embedding
│   ├── 1.2.4.2 Hidden State 是什么
│   ├── 1.2.4.3 同一 Token 在不同上下文中的变化
│   └── 1.2.4.4 表示在层与层之间怎样传递
│
└── 1.2.5 位置机制
    ├── 1.2.5.1 为什么必须表示顺序
    ├── 1.2.5.2 Absolute Position Embedding
    ├── 1.2.5.3 RoPE 的作用位置与直觉
    ├── 1.2.5.4 ALiBi 的作用位置与直觉
    └── 1.2.5.5 三类位置机制对比
```

## 本专题边界

### 本专题负责

- 向量的最低必要直觉；
- Token ID 到初始向量的映射；
- Embedding 作为模型参数的静态属性；
- 初始 Embedding 与上下文化 Hidden State 的概念边界；
- 三类位置机制的目的和作用位置。

### 本专题暂不展开

- Q、K、V 怎样计算；
- Attention Score 和 Attention Weight；
- RoPE 的旋转矩阵推导；
- RoPE 与 ALiBi 的完整数学推导；
- Transformer Block 内部怎样更新 Hidden State；
- LM Head、Logits 和下一个 Token 概率；
- Embedding 检索、向量数据库和 RAG。

这些内容分别属于后续的 Attention、Transformer Block、输出机制和 Agent/RAG 工程。

## 必须保持的概念边界

```text
Token ID：词表索引
Token Embedding：查表得到的初始参数向量
Position Mechanism：让计算获得顺序或相对距离信息
Hidden State：经过模型层处理后不断变化的表示
```

- Embedding 不是 Tokenizer 的输出；Tokenizer 输出 Token ID。
- Embedding Matrix 属于 LLM 参数，不属于 Tokenizer 词表文件。
- 初始 Embedding 不等于完整、固定的“词义向量”。
- Hidden State 不等于模型永久记忆，也不等于最终答案。
- 不能把所有位置机制都讲成“位置向量加到 Token 向量上”：Absolute Position 常这样做，RoPE 作用于 Attention 的 Q/K，ALiBi 作用于 Attention Score。

## 阶段边界

本专题只建立模型的共同静态结构：Token ID 怎样查表、向量形状是什么、初始表示与 Hidden State 如何区分。Embedding 在普通运行和训练中的完整过程分别跳转到：

- [[01-LLM/02-LLM普通运行与生成/LLM普通运行与生成大纲|LLM 普通运行与生成]]；
- [[01-LLM/03-LLM训练与对齐/LLM训练与对齐大纲|LLM 训练与对齐]]。

## 完成标准

完成本专题后，应当能够：

1. 解释 Token ID 已经是数字，为什么仍不能当作语义数值使用；
2. 推演 Token ID 如何通过 Embedding Matrix 变成向量；
3. 解释 `vocab_size × hidden_size` 与 Embedding 参数量的关系；
4. 说明 Embedding 是模型参数，并知道训练与普通运行的详细流程位于各自模块；
5. 区分初始 Token Embedding 与上下文化 Hidden State；
6. 说明模型没有位置信息时会缺失什么；
7. 指出 Absolute Position、RoPE、ALiBi 分别作用在哪里；
8. 带着正确输入表示进入下一专题 Causal Self-Attention。

## 当前进度

- [x] [[1.2.1-向量基础概览|1.2.1 向量基础]]
- [x] [[1.2.2-Token-Embedding概览|1.2.2 Token Embedding]]
- [x] [[1.2.3-Embedding参数与语义边界概览|1.2.3 Embedding 参数与语义边界]]
- [x] [[1.2.4-初始表示与Hidden-State概览|1.2.4 初始表示与 Hidden State]]
- [x] [[1.2.5-位置机制概览|1.2.5 位置机制]]

本专题已完成。下一专题：[[1.3-Causal-Self-Attention概览|1.3 Causal Self-Attention]]。
